#!/bin/sh

VER="$1"
DIR="$2"

if [ ! "$#" -eq "2" ]; then
  echo "usage: $0 [version] [path]"
  exit 1
fi

FILE="aerofs-$VER-public.map"
MAP_FILE="$DIR"/"$FILE"

if [ ! -f "$MAP_FILE" ]; then
    echo "retrieving map file..."
    scp sv.aerofs.com:/maps/"$FILE" "$MAP_FILE"
fi
