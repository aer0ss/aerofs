#!/bin/bash

if [ x"$1" == x ]; then
    echo usage: '$0 email_address'
    exit
fi

ARG="--no-check-certificate"

ENCODED=`echo $1 | sed -e 's/+/%2B/g'`

wget -q $ARG -O- "https://sp-comcast.aerofs.com/sp?aerofs=love&fromPerson=Yuri+Sagalov&to=$ENCODED"
