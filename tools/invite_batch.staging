#!/bin/bash

if [ x"$1" == x ]; then
    echo "usage: $0 <invite_count>"
    exit
fi

ARG="--no-check-certificate"

wget -q $ARG -O- "https://staging.aerofs.com/sp/sp?aerofs=lotsoflove&inviteCount=$1"
