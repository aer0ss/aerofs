#!/bin/bash

grep -q "\[MYSQL_PASSWORD\]" "../../java/aerofs.spsv/resources/WEB-INF/web.xml"

if [ $? == 0 ]; then
    echo "You need to update the mysql password before deploying"
    exit 1
else
    exit 0
fi
