Vagrant.configure(2) do |config|
  config.vm.guest = :windows
  config.vm.communicator = "winrm"
  config.vm.base_mac = "UPDATE_ME"

  config.vm.provider "virtualbox" do |vb|
    vb.gui = true
  end
end
