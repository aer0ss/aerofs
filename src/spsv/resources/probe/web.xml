<?xml version="1.0" encoding="UTF-8"?>

<web-app>
  <!-- Default page to serve -->
  <welcome-file-list>
    <welcome-file>index.html</welcome-file>
  </welcome-file-list>

  <!-- Database definitions -->
  <context-param>
    <param-name>sp_database_resource_reference</param-name>
    <param-value>jdbc/SPDatabase</param-value>
  </context-param>
  <resource-ref>
    <res-ref-name>jdbc/SPDatabase</res-ref-name>
    <res-type>javax.sql.DataSource</res-type>
    <res-auth>Container</res-auth>
  </resource-ref>

  <!-- To enforce serializable sessions. -->
  <distributable/>

  <!-- Initialization listener -->
  <listener>
    <listener-class>com.aerofs.sp.server.listeners.ProbeLifecycleListener</listener-class>
  </listener>

  <!-- Servlets -->

  <!-- Team Server: probe sanity of Team Server installations -->
  <servlet>
    <servlet-name>ServletTeamServerProbe</servlet-name>
    <servlet-class>com.aerofs.sp.server.TeamServersProbeServlet</servlet-class>
  </servlet>

  <!-- URL mapping -->
  <servlet-mapping>
    <servlet-name>ServletTeamServerProbe</servlet-name>
    <url-pattern>/team-servers</url-pattern>
  </servlet-mapping>
</web-app>
