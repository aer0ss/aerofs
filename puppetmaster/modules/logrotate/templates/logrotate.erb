<% if compress -%>
compress
<% end %>

<%= filename %> {
    rotate <%= quantity %>
    # use copytruncate to rotate the log without making
    # other applications reopen the file.
    copytruncate
    <%= frequency %>
    size <%= size %>
}
