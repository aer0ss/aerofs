[app:main]
use = egg:aerofs-web

# Mode
deployment.mode = public
deployment.oauth_server_uri = http://api.aerofs.com:8700

# SP settings
base.sp.url = https://sp.aerofs.com/sp
# When updating SP version make sure to update BaseParam.java
sp.version = 21

# Pyramid settings
pyramid.reload_templates = false
pyramid.debug_authorization = false
pyramid.debug_notfound = false
pyramid.debug_routematch = false
pyramid.default_locale_name = en
mako.directories = web:templates
# Where static assets are hosted
static.prefix = https://d37fxzwppxbuun.cloudfront.net
# Where installer packages are hosted (aka cache.client.aerofs.com)
# Note that using https://cache... would cause the browser to complain cert errors
installer.prefix = https://dsy5cjk52fz4a.cloudfront.net

# Session factory settings
# see http://beaker.readthedocs.org/en/latest/configuration.html#session-options
# and http://beaker.readthedocs.org/en/latest/modules/session.html for more options
session.type = cookie
session.key = aerofs_session
session.secure = True
# The cookie should only be accessible by the browser not by JavaScript.
# This is to prevent the session token from being stolen via XSS attacks.
session.httponly = True
# 1 year. Session timeouts are handled by SP.
session.timeout = 31536000
session.validate_key = woo4tahruov9ofee2phuNgahdahNg8Tuc9thah7ahdah5aip
session.encrypt_key = zaich8Phetha4thee0EDieZ8seh2du6Keeng5ieZei9pheev

# these must match what's in the mysql db on api.aerofs.com, specifically the result
# of "select secret from client where clientId='aerofs-shelob'" in db "bifrost"
oauth.shelob_client_secret = 6c9c8cb9-a286-42dd-8e70-684855e93676
oauth.zelda_client_secret = b139a627-bd22-4e61-b615-1432f4347337

segmentio.api_key = 5h3jaxxnqd
segmentio.secret_key = ez2r32cfwejkql9qeitg

[server:main]
use = egg:waitress#main
host = 0.0.0.0
port = 6543

[uwsgi]
virtualenv = /opt/web/pythonenv
wsgi-file = /opt/web/production.wsgi
socket = :8080
master = true

# Set these based on capacity of server, but because some of
# our code is stateful (namely in-memory session storage) more
# processes could cause problems
processes = 1
listen = 256

chdir = /opt/web
plugins = python
single-interpreter = true

# harakiri settings (kills requests taking longer than 30s
# and rogue workers)
harakiri = 30
harakiri-verbose = true
post-buffering = 32768
reload-on-as = 256
reload-on-rss = 128
no-orphans = true

[loggers]
keys = root, web

[handlers]
keys = console, filelog

[formatters]
keys = generic

[logger_root]
level = DEBUG
handlers = console

[logger_web]
level = DEBUG
handlers = filelog
qualname = web

[handler_filelog]
class = FileHandler
args = ('/var/log/web/web.log','a')
level = DEBUG
formatter = generic

[handler_console]
class = StreamHandler
args = (sys.stderr,)
level = NOTSET
formatter = generic

[formatter_generic]
format = %(levelname)-5.5s %(asctime)s [%(threadName)s] [%(name)s] %(message)s
