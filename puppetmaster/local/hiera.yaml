# this is the mocked out hiera data for local testing of puppet
:backends:
    - yaml

:logger:
    - console

:hierarchy:
    - %{hostname}
    - users
    - common
:yaml:
    :datadir: '/etc/puppet/hieradata'
