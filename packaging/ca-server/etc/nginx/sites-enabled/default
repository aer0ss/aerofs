server {
    listen 1029 default;
    server_name ca;

    # TODO (MP) I would eventually like to see this prefix go away, once we
    # ditch staging entirely.
    location /prod {
        proxy_pass http://localhost:9002;
    }
}
